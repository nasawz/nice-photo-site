import Image from "next/image";
import { Card, Cards } from "nextra-theme-docs";
// import turbo from "public/showcase/turbo.jpg";

{<h1 className="mt-10 mb-4 text-center text-[2.5rem] font-bold tracking-tight">案例展示</h1>}

{<p className="mb-16 text-center text-lg text-gray-500 dark:text-gray-400">以下作品由 NicePhoto 提供支持</p>}

<Cards>
  {[
    {
      name: "咖啡风味魔法",
      url: "https://flavor-magic.np.xiangshi.app/",
      {/* image: turbo, */}
    },
  ].map(({ name, url, image }) => (
    <Card key={name} image arrow target="_blank" title={name} href={url}>
      {/* <Image
        src={image}
        alt={`${name} preview`}
        className="object-cover [aspect-ratio:12/6.3]"
      /> */}
    </Card>
  ))}
</Cards>
